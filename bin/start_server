#!/usr/bin/env ruby
$:.unshift(File.expand_path(File.dirname(__FILE__) + "/../lib/"))

require 'server'

port = ARGV[0] || 5000
server_config = { host: 'localhost', port: port }
HTTPServer.new(server_config).run
