#!/usr/bin/env ruby
$:.unshift(File.expand_path(File.dirname(__FILE__) + "/../lib/"))

require 'server'
require 'response_message_presenter'

server_config = { host: 'localhost', port: 5000, io: ResponseMessagePresenter.new }
HTTPServer.new(server_config).run
